# 1Panel 部署步骤 - 120.53.242.78

## 服务器信息
- **服务器 IP**: 120.53.242.78
- **数据库地址**: 120.53.242.78:3306
- **数据库名**: jay_chat
- **访问地址**: http://120.53.242.78

---

## 步骤 1：准备项目文件

### 1.1 在本地修改配置

#### 修改前端配置
编辑 `client/.env.production`（如果不存在则创建）：
```env
VITE_API_BASE_URL=http://120.53.242.78:8080
VITE_WS_BASE_URL=http://120.53.242.78:8080
```

#### 修改后端 CORS 配置
编辑 `src/main/java/com/sunrisejay/jaychat/config/SecurityConfig.java`，在 `corsConfigurationSource()` 方法中添加你的服务器地址：
```java
configuration.setAllowedOrigins(Arrays.asList(
    "http://localhost:5173", 
    "http://localhost:3000", 
    "http://127.0.0.1:5173",
    "http://120.53.242.78",        // 添加这行
    "http://120.53.242.78:80"      // 添加这行
));
```

#### 修改后端数据库配置（如果需要）
`application.properties` 中的数据库配置已经是正确的：
```properties
spring.datasource.url=jdbc:mysql://120.53.242.78:3306/jay_chat?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
spring.datasource.username=root
spring.datasource.password=123123
```

### 1.2 构建项目

#### 构建后端
```bash
cd F:\code\jay-chat
mvn clean package -DskipTests
```
构建完成后，会在 `target` 目录生成 `jay-chat-0.0.1-SNAPSHOT.jar`

#### 构建前端
```bash
cd client
npm install
npm run build
```
构建完成后，会在 `client/dist` 目录生成前端静态文件

---

## 步骤 2：上传文件到服务器

### 方法一：使用 1Panel 文件管理
1. 登录 1Panel
2. 进入 **文件** → 创建目录，例如：`/opt/jay-chat`
3. 上传以下文件：
   - `target/jay-chat-0.0.1-SNAPSHOT.jar`（后端 JAR 文件）
   - `client/dist/` 目录下的所有文件（前端静态文件）

### 方法二：使用 SCP 或 FTP
```bash
# 上传后端 JAR
scp target/jay-chat-0.0.1-SNAPSHOT.jar root@120.53.242.78:/opt/jay-chat/

# 上传前端文件
scp -r client/dist/* root@120.53.242.78:/opt/jay-chat/frontend/
```

---

## 步骤 3：初始化数据库

### 3.1 在 1Panel 中执行 SQL

1. 登录 1Panel
2. 进入 **数据库** → **MySQL**
3. 找到你的数据库实例，点击 **管理** 或 **SQL 编辑器**
4. 选择数据库 `jay_chat`
5. 执行以下 SQL（复制 `database/init_tables_complete.sql` 的内容）：

```sql
USE jay_chat;

CREATE TABLE IF NOT EXISTS `user` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `username` VARCHAR(64) NOT NULL UNIQUE COMMENT '登录用户名',
  `password` VARCHAR(255) NOT NULL COMMENT '加密后的密码',
  `nickname` VARCHAR(64) NOT NULL COMMENT '昵称',
  `avatar` VARCHAR(255) DEFAULT NULL COMMENT '头像 URL',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `chat_session` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `type` VARCHAR(16) NOT NULL COMMENT 'single / group',
  `name` VARCHAR(128) DEFAULT NULL COMMENT '会话名（群聊用）',
  `owner_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '创建者',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_type_name` (`type`, `name`),
  KEY `idx_owner` (`owner_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `chat_session_member` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `session_id` BIGINT UNSIGNED NOT NULL,
  `user_id` BIGINT UNSIGNED NOT NULL,
  `joined_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_session_user` (`session_id`,`user_id`),
  KEY `idx_session` (`session_id`),
  KEY `idx_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS `chat_message` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `session_id` BIGINT UNSIGNED NOT NULL,
  `sender_id` BIGINT UNSIGNED NOT NULL,
  `content` TEXT NOT NULL,
  `content_type` VARCHAR(32) NOT NULL DEFAULT 'text',
  `sent_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_session_time` (`session_id`,`sent_at`),
  KEY `idx_sender` (`sender_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

## 步骤 4：部署后端（Spring Boot）

### 4.1 在 1Panel 中创建 Java 应用

1. 登录 1Panel
2. 进入 **应用商店** → **运行环境** → **Java**
3. 如果没有安装 Java，先安装 **OpenJDK 8** 或 **OpenJDK 11**
4. 进入 **网站** → **Java 项目** → **创建**
5. 填写以下信息：
   - **项目名称**: jay-chat-backend
   - **运行端口**: 8080
   - **运行方式**: JAR
   - **JAR 文件路径**: `/opt/jay-chat/jay-chat-0.0.1-SNAPSHOT.jar`
   - **运行命令**: `java -jar /opt/jay-chat/jay-chat-0.0.1-SNAPSHOT.jar`
   - **工作目录**: `/opt/jay-chat`

### 4.2 配置环境变量（如果需要）

在 1Panel 的 Java 项目设置中，可以添加环境变量：
- `SPRING_DATASOURCE_URL`（如果需要覆盖配置文件）
- `JAYCHAT_JWT_SECRET`（如果需要修改 JWT 密钥）

### 4.3 启动后端服务

1. 在 1Panel 中点击 **启动**
2. 查看日志，确认服务启动成功
3. 测试访问：`http://120.53.242.78:8080/doc.html`（Knife4j 文档）

---

## 步骤 5：部署前端（Vue 3）

### 5.1 在 1Panel 中创建静态网站

1. 登录 1Panel
2. 进入 **网站** → **创建网站**
3. 选择 **静态网站**
4. 填写以下信息：
   - **网站名称**: jay-chat
   - **域名**: `120.53.242.78`（或你的域名）
   - **网站目录**: `/opt/jay-chat/frontend`
   - **PHP 版本**: 不需要（静态网站）

### 5.2 配置 Nginx 反向代理

在 1Panel 的网站设置中，找到 **Nginx 配置**，添加以下配置：

```nginx
server {
    listen 80;
    server_name 120.53.242.78;  # 或你的域名
    
    root /opt/jay-chat/frontend;
    index index.html;

    # 前端路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 代理到后端
    location /api {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # WebSocket 代理
    location /ws {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }

    # Knife4j 文档代理（可选）
    location /doc.html {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

### 5.3 重启 Nginx

在 1Panel 中点击 **重启** 网站，或使用命令：
```bash
nginx -s reload
```

---

## 步骤 6：配置防火墙

### 6.1 在 1Panel 中配置防火墙

1. 登录 1Panel
2. 进入 **安全** → **防火墙**
3. 确保以下端口已开放：
   - **80**：HTTP（前端访问）
   - **8080**：后端 API（如果直接访问）
   - **3306**：MySQL（建议仅内网访问，不要对外开放）

### 6.2 如果使用云服务器安全组

在云服务器控制台配置安全组规则：
- 允许 80 端口（HTTP）
- 允许 8080 端口（后端 API，可选）
- 禁止 3306 端口对外访问（数据库安全）

---

## 步骤 7：验证部署

### 7.1 测试前端访问
访问：`http://120.53.242.78`
- 应该能看到登录页面
- 可以正常注册和登录

### 7.2 测试后端 API
访问：`http://120.53.242.78:8080/doc.html`
- 应该能看到 Knife4j API 文档

### 7.3 测试聊天功能
1. 注册两个账号
2. 登录后进入公共聊天室
3. 发送消息，确认消息能正常发送和接收

---

## 步骤 8：常见问题排查

### 问题 1：前端无法连接后端
**检查**：
1. 后端服务是否启动（查看日志）
2. Nginx 配置是否正确
3. 防火墙是否开放 8080 端口
4. 前端 `.env.production` 配置是否正确

### 问题 2：WebSocket 连接失败
**检查**：
1. Nginx 配置中的 `/ws` 代理是否正确
2. 后端 WebSocket 配置是否正确
3. 防火墙是否允许 WebSocket 连接

### 问题 3：CORS 跨域错误
**检查**：
1. `SecurityConfig.java` 中的 CORS 配置是否包含服务器地址
2. 前端请求的 URL 是否正确

### 问题 4：数据库连接失败
**检查**：
1. MySQL 服务是否运行
2. 数据库用户名密码是否正确
3. 数据库是否已创建表结构

---

## 步骤 9：生产环境优化建议

### 9.1 安全配置
1. **修改 JWT 密钥**：在 `application.properties` 中修改 `jaychat.jwt.secret` 为强随机字符串
2. **使用 HTTPS**：在 1Panel 中配置 SSL 证书（Let's Encrypt 免费证书）
3. **数据库密码**：使用强密码，不要使用默认密码
4. **防火墙**：只开放必要端口，关闭不必要的服务

### 9.2 性能优化
1. **Nginx 缓存**：配置静态资源缓存
2. **数据库索引**：确保数据库表有适当的索引
3. **JVM 参数**：优化 Java 应用的内存配置

### 9.3 监控和日志
1. **日志管理**：配置日志轮转，避免日志文件过大
2. **监控告警**：配置服务监控，及时发现问题

---

## 快速部署命令（SSH）

如果你熟悉命令行，可以使用以下命令快速部署：

```bash
# 1. 创建目录
mkdir -p /opt/jay-chat/frontend

# 2. 上传文件（使用 scp 或 1Panel 文件管理）
# 上传 jay-chat-0.0.1-SNAPSHOT.jar 到 /opt/jay-chat/
# 上传前端文件到 /opt/jay-chat/frontend/

# 3. 启动后端（使用 nohup 或 systemd）
cd /opt/jay-chat
nohup java -jar jay-chat-0.0.1-SNAPSHOT.jar > app.log 2>&1 &

# 4. 检查服务
ps aux | grep java
netstat -tlnp | grep 8080
```

---

## 完成！

部署完成后，你可以：
- 访问前端：`http://120.53.242.78`
- 访问 API 文档：`http://120.53.242.78:8080/doc.html`
- 开始使用聊天室功能

如有问题，请查看日志文件或联系我！
